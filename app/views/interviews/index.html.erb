<h1><%= @user[:user_name] %>の面接一覧</h1>

<% if current_user.role == "recruter" %>
  <%= render 'interviews_recruter', interviews: @interviews %>
<% else %>
  <%= render 'interviews_guest', interviews: @interviews %>
<% end %>
