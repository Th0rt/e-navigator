<h1><%= @user[:user_name] %>の過去の面接一覧</h1>

<table>
  <thead>
    <th>日程</th>
    <th>ステータス</th>
  </thead>
  <% @interviews.each do | interview | %>
    <tr>
      <td><%= interview.date.strftime("%Y/%m/%d %H:%M") %></td>
      <td><%= interview.status %></td>>
      <% end %>
    </tr>
  <% end %>
</table>
